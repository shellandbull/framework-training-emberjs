import { isMario } from 'demo-app/helpers/is-mario';
import { module, test } from 'qunit';

module('Unit | Helper | is mario');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isMario([42]);
  assert.ok(result);
});
